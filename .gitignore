# Local-only HTML/CSS template references (not part of @opus/web runtime; keeps clones/Docker small)
Web_Template/

# Dependencies
node_modules
.pnp
.pnp.js
.pnpm-store/

# Build / cache
.turbo
.next
out
dist
build
*.tsbuildinfo

# Expo / React Native
.expo
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Environment — never commit secrets (GCP Secret Manager per SECURITY_GOVERNANCE.md)
.env
.env.local
.env.*.local
!.env.example
!**/.env.*.example

# OAuth client secret JSON downloaded from Google Cloud Console — never commit.
# ISO 27001 A.9.4.5 / A.10.1.2 (§3) — keep credential files outside VCS; see SECURITY_GOVERNANCE.md.
client_secret*.json

# Docker DB volumes & env (Mac Mini / local; never commit secrets)
infrastructure/.env
infrastructure/data/
infrastructure/docker/.env
infrastructure/docker/data/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / IDE
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

# Local-only artwork drops (do not commit licensed/copyrighted covers)
apps/web/public/local-artworks/

# Server-side local storage (dev-only artifacts)
apps/web/storage/

# Coverage
coverage

# Terraform (state may contain secrets; keep local/backend out of VCS)
**/.terraform/
*.tfstate
*.tfstate.*
crash.*.log
**/terraform.tfvars
