# Build cache & deps (reinstalled in image)
**/node_modules
**/.next
**/.turbo
**/dist

# VCS & editor
.git
.github
.cursor

# Secrets — never copy into build context (standalone must not embed .env)
.env
.env.*
**/.env
**/.env.*
!**/.env.example
!**/.env.*.example

# Not needed for @opus/web image (large local HTML/CSS reference tree; fills small EC2 root + Docker layers)
Web_Template/

# Not needed for @opus/web image
apps/mobile
infra/terraform/.terraform
infrastructure/data
**/coverage
*.log
